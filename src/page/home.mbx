using @dom { trait View }
using @component { counter_btn }

pub fn home() -> &View {
  <div>

    <h1>Welcome to Aitne</h1>

    <div class="container">
        <CounterBtn />
        <CounterBtn incr={-1}/>
    </div>
  </div>
}
